# Core
Django==5.2.16
gunicorn==26.0.0

# Database
# SQLite is built into Python — no extra package needed for local dev.
# psycopg2-binary is only imported when SUPABASE_DB_HOST is set in .env
# (see config/settings.py) — needed for the Render+Supabase deployment path.
# Pinned to 2.9.12+ because that's the first release with prebuilt wheels
# for Python 3.14 — older pins force pip to compile from source, which
# needs PostgreSQL's pg_config and fails on a machine that doesn't have it.
psycopg2-binary==2.9.12

# Object storage (Supabase Storage, S3-compatible) — only used when
# SUPABASE_STORAGE_BUCKET is set in .env. See config/settings.py.
# MUST stay >=1.14.5 — 1.14.4 shipped a regression where .exists() returns
# False for a file that was just saved and genuinely exists (upstream
# issue jschneier/django-storages#1430, reverted in 1.14.5's #1484/#1486).
# That exact bug is what made every sticker application submission fail
# with "required documents could not be found" once documents moved to S3.
django-storages==1.14.6
boto3==1.35.99

# Real email delivery over HTTPS (works on Render's free tier, unlike SMTP).
# Only active when EMAIL_BACKEND=anymail.backends.brevo.EmailBackend in .env.
# See config/settings.py.
django-anymail==15.1

# Auth & Security
argon2-cffi==25.1.0
django-axes==8.3.1
django-ratelimit==4.1.0

# Static files
whitenoise==6.12.0

# Forms
django-crispy-forms==2.5
crispy-bootstrap5==2026.3

# Files & Documents
Pillow==12.1.1
reportlab==4.4.10

# Config
python-decouple==3.8
